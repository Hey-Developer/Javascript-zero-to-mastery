/* 
$ Window Events:
  You can bind events with window.
  Not all events work with window for ex- onclick event will not work with window.

~ Event Binding with HTML Attributes.
  <body onload="alert('window_opened');">
  > This event will fire when window loads.

~ Event Binding with JavaScript.
  window.addEventListener('load','function',phase)

 ~ Also window.onload=handler
 
*/